# AIPICSQR Photographer Node - Dependencies
# Python 3.10 - 3.13 required (3.12 recommended)
# Install via Run-AIPICSQR.bat — do not run pip install manually.

# Core
opencv-contrib-python>=4.8.0,<5.0  # YuNet + SFace; pinned major to avoid ABI breaks
numpy>=1.24.0,<3.0
Pillow>=10.0.0,<12.0               # Image compression

# Supabase — pinned to avoid pulling in a storage3 that requires MSVC (pyiceberg)
supabase>=2.0.0,<2.10.0
storage3>=0.7.0,<2.30.0            # 2.30.0 introduced pyiceberg (needs MSVC on Windows)

# Folder watching
watchdog>=3.0.0                    # File system events

# System monitoring
psutil>=5.9.0                      # CPU/memory/temp monitoring

# Environment
python-dotenv>=1.0.0               # .env file loading

# Utilities
requests>=2.31.0                   # HTTP requests
