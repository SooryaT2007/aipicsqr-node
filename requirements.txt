# AIPICSQR Photographer Node - Dependencies
# Python 3.10 - 3.13 required (3.12 recommended)
# Install via Run-AIPICSQR.bat — do not run pip install manually.

# Computer vision (face detection + recognition)
opencv-contrib-python>=4.8.0,<5.0  # YuNet + SFace
numpy>=1.24.0,<3.0

# Image compression
Pillow>=10.0.0,<12.0

# Folder watching
watchdog>=3.0.0

# System monitoring (CPU/temp)
psutil>=5.9.0

# HTTP — all API calls go through requests; no Supabase SDK on the node
requests>=2.31.0
